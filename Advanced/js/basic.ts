let something = []
let object = { a: false, b: true, c: true }

// for (let key in object) {
//   something.push(() => console.log(key))
// }
function what(){

for (let whatever in object) {
		let sum = 2+2;
  something.push(() => console.log(sum))
}
}
something.forEach(item => what());

